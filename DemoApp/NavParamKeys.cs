namespace DemoApp
{
    /// <summary>
    ///     Location of the keys used for navigation parameters. Prevents fat-fingering strings.
    /// </summary>
    public static class NavParamKeys
    {
        #region Keys

        public const string RequiredDemoNavParam = nameof(RequiredDemoNavParam);
        public const string ThrowError = nameof(ThrowError);

        #endregion
    }
}